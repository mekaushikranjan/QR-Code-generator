{
  "name": "qr-code generator",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "engines": {
    "node": "v20.17.0" // or specify the version you are using
  },
  "dependencies": {
    "express": "^4.x",
    "qr-image": "^3.x"
  }
}

{
  "name": "qr-code generator",
  "scripts": {
    "build": "babel src -d lib",
    "test": "jest"
  }
}
